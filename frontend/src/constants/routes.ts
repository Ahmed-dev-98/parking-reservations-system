export const EAPI = {

    LOGIN: '/auth/login',
    GATES: '/master/gates',
    ZONES: '/master/zones'
}


export const EROUTES = {
    LOGIN: '/login',
    CHECKPOINT: '/checkpoint',
    ADMIN: '/admin',
    GATE: '/gates/:id',
    GATES: '/gates',
    HOME : '/'
}